<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/DayNightColor</item>
        <item name="colorAccent">@android:color/darker_gray</item>
        <item name="colorOnSurface">@color/DayNightColor</item>

        <!--Пока не знаю зачем нужны и где могут понадобиться-->
        <!--
        <item name="colorControlActivated">@color/DayNightColor</item>
        <item name="colorControlHighlight">@color/DayNightColor</item>
        <item name="colorControlNormal">@color/DayNightColor</item>
        -->

        <item name="colorPrimaryDark">@color/backgroundColor</item>
        <item name="android:colorBackground">@color/backgroundColor</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>

        <!-- Анимация перехода между активностями -->
        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowEnterTransition">@transition/explode</item>
        <item name="android:windowExitTransition">@transition/explode</item>
    </style>

    <style name="DarkTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Add attributes here -->
        <item name="colorAccent">@android:color/darker_gray</item>
        <item name="colorPrimaryDark">@color/backgroundColor</item>
        <item name="android:colorBackground">@color/backgroundColor</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>

    <style name="MyEditTextTheme">
        <item name="android:colorControlNormal">@color/editTextColor</item>
        <item name="android:colorControlActivated">@android:color/white</item>
        <item name="android:colorControlHighlight">@android:color/white</item>
    </style>

    <style name="DrawerArrowStyle" parent="@style/Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@color/textColor</item>
    </style>

</resources>
